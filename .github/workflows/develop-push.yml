name: On push Actions

on:
  push:
    branches:
      - development

jobs:
  build:
    name: Build the beamer PDF  image
    runs-on: ubuntu-24.04
    container:
      image : ghcr.io/ichmy55/opencae-slides:main
      options: --user 1001  --group 118
    steps:
      - name: "Test"
        run:  |
          echo ---
          pwd
          echo ---
          who am i
          echo ---
          ls -al
          echo ---
          ls -al ../
          echo ---
          cat /etc/passwd
          echo ---
          who
          echo ---
          echo qwe > qwe.txt
          echo ---
          ls -al
          echo ---
      - name: "Checkout  repository"
        uses: actions/checkout@v4
      - name: "Test2"
        run:  |
          echo ---
          ls -al
          echo ---
