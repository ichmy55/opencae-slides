name:  JOBS at push to main

on:
  push:
    branches:
      - main

jobs:
  build-pdf:
    uses: ./.github/workflows/build-pdf.yml

  package-textcomp:
    uses: ./.github/workflows/package-textcomp.yml

  package-textlint:
    uses: ./.github/workflows/package-textlint.yml
